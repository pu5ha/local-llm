// Plain (no "server-only" guard) so this can be imported from client
// components — the channel username is public and meant to be shared,
// unlike the bot token. Keep in sync with TELEGRAM_CHANNEL_ID (@localainews).
export const TELEGRAM_CHANNEL_URL = "https://t.me/localainews";
